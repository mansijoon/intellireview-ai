name: IntelliReview

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run deterministic tests
        run: |
          python -m pytest -q test_review.py

      - name: Repository analysis
        run: |
          python -m intellireview_cli analyze . --json > intellireview-result.json

      - name: Quality gate
        run: |
          python - <<'PY'
          import json

          with open("intellireview-result.json") as f:
              result = json.load(f)

          if result["status"] != "success":
              raise SystemExit(
                  "IntelliReview quality gate failed: "
                  + result["status"]
              )

          if result["successful_count"] != result["analyzers"].__len__():
              raise SystemExit(
                  "IntelliReview quality gate failed: "
                  "not all analyzers succeeded"
              )

          print("IntelliReview quality gate: PASS")
          PY
